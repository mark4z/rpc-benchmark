version: '3'
services:
  #http
  http:
    build:
      context: .
      dockerfile: ./http/Dockerfile
    ports:
      - "8080:8080"
    networks:
      - local
    deploy:
      resources:
        limits:
          cpus: '1'
          memory: 512M
  java:
    build:
      context: .
      dockerfile: ./java/Dockerfile
    ports:
      - "8081:8080"
    networks:
      - local
    deploy:
      resources:
        limits:
          cpus: '1'
          memory: 2048M

networks:
  local:
    driver: bridge